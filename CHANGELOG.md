# 更新日志

## 2026-09-13 — 客户端兼容与构建更新

- 修复 GitHub Runner 测试用户对 `/media` 夹具目录无写权限导致的流水线失败；仅在临时 CI 主机准备目录。
- 同步 iOS 表单登录、客户端服务端地址及缩略图兼容修复，并加入回归测试。
- 同步媒体目录访问范围、分页和管理接口修复。
- 将多语言 JSON 资源纳入二进制嵌入和 Docker 构建白名单。
- 保留 Linux x64（amd64）和 ARM64（aarch64）独立二进制、SHA256 校验与多架构镜像流水线。
- 保留授权地址环境变量覆盖及默认地址，补充默认地址和 TLS 配置测试。
- 加强部署环境文件、凭据和修复备份排除；发布内容不包含生产配置、密钥、证书、数据库或日志。

## 2026-09-13 — 初始发布

- 同步最新网关播放、错误日志及管理页面修复。
- 发布 Linux amd64（x64）和 arm64（aarch64）镜像，Compose 自动选择主机架构。
- 前端、Go 编译和运行环境分阶段构建；仅将二进制复制到运行镜像。
- CI 使用两个原生架构构建节点，完整数据库测试通过后才发布统一镜像标签。
- CI 同时导出两种架构的二进制和 SHA256 校验文件。
- 授权地址、密码及密钥通过 .env 注入，移除私有地址和证书；兼容授权服务器免费模式。
- 构建上下文采用白名单，提交检查排除密钥、真实公网 IP、数据库、备份和迭代产物。
- 用户完成首次配置后，可通过 `docker compose pull && docker compose up -d` 更新。
